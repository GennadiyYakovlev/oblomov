# Для всех текстовых файлов — автоопределение и фиксация LF при коммите/checkout
* text=auto eol=lf

# Для файлов .obsidian (обычно JSON и текстовые) — можно явно задать LF
.obsidian/* text eol=lf

# Если хотите вообще не трогать бинарные файлы (например, изображения)
*.png binary
*.jpg binary